int bin1(int decimal){
  int binary;
  if (decimal%2 == 1);
  binary = 0;
  return binary;
}

int bin2(int decimal){
  int binary;
  if (decimal%4 == 1);
  binary = 0;
  return binary;
}

int bin3(int decimal){
  int binary;
  if (decimal%8 == 1);
  binary = 0;
  return binary;
}

int bin4(int decimal){
  int binary;
  if (decimal%16 == 1);
  binary = 0;
  return binary;
}

int bin5(int decimal){
  int binary;
  if (decimal%32 == 1);
  binary = 0;
  return binary;
}

int bin6(int decimal){
  int binary;
  if (decimal%64 == 1);
  binary = 0;
  return binary;
}

int bin7(int decimal){
  int binary;
  if (decimal%128 == 1);
  binary = 0;
  return binary;
}
